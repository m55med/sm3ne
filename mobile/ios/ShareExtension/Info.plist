<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>بصوتك</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<!-- Must track the host app exactly or App Store Connect rejects the
	     bundle. Use the same Flutter-driven build variables as Runner. -->
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<!-- The extension transcribes the shared voice note on-device via Apple
	     Speech before any server fallback, so it needs its own speech-usage
	     description (extensions don't inherit the host app's). -->
	<key>NSSpeechRecognitionUsageDescription</key>
	<string>يستخدم بصوتك التعرّف على الصوت داخل الجهاز لتحويل الرسالة الصوتية المُشارَكة إلى نص بدون رفعها إلى الخادم.</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Use an explicit NSPredicate rule (not the dict form). The dict
			     form's "File"/"Movie" keys do NOT reliably match a shared voice
			     note (and "...SupportsAttachmentsWithMaxCount" is not a real key,
			     so iOS ignored it) — which is why the extension never appeared in
			     the share sheet. This predicate matches a single attachment that
			     conforms to audio, movie (Messenger/iOS sometimes wrap voice in
			     mp4), or generic data (some apps export the note as a plain file).
			     ShareViewController.swift still filters by UTI + extension. -->
			<key>NSExtensionActivationRule</key>
			<string>SUBQUERY (
				extensionItems,
				$extensionItem,
				SUBQUERY (
					$extensionItem.attachments,
					$attachment,
					ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.audio"
					|| ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.movie"
					|| ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.data"
				).@count &gt;= 1
			).@count == 1</string>
		</dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
	</dict>
</dict>
</plist>
